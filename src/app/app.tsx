import type { FC } from 'react';
import RepositoryCards from '../entities/repositories/ui/repository-cards';

const App: FC = () => {
  return (
    <>
      <RepositoryCards />
    </>
  );
};

export default App;
